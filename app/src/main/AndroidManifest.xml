<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实时日志悬浮窗（Dialog 方案已不需要，保留兼容旧 UI） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 用户头像下载（模块本体独立进程） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:multiArch="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoOralCalculation">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- 配置导出/导入的透明承载 Activity（对齐 WeKit TransparentActivity）：
             注入面板是 ComponentDialog 无 ActivityResultRegistry，经本透明 Activity
             启动安卓官方文件选取工具（SAF）。exported=true 以便宿主进程拉起。 -->
        <activity
            android:name=".ui.ConfigTransferActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>

</manifest>
